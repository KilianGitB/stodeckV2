{% extends 'base.html.twig' %}

{% block title %}Stodeck - Home{% endblock %}

{% block body %}
     <header class="masthead">
            <div class="container px-4 px-lg-5 d-flex h-100 align-items-center justify-content-center">
                <div class="d-flex justify-content-center">
                    <div class="text-center">
                        <a class="btn btn-primary" href="{{ path('app_article_new') }}">Enregistrer</a>
                        <a class="btn btn-primary" href="{{ path('app_invoice_new') }}">Distribuer </a>
                    </div>
                </div>
            </div>
        </header>
{% endblock %}
